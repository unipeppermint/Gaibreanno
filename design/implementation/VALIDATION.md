# 验证记录

日期：2026-09-23

## 卡组与图鉴分工优化

- Debug 模拟器构建通过；配牌与收藏册改为各自独立的页面构建逻辑。
- SE 实际验证：以幼龙替换巨龙后仍为 6 张，平均费用从 1.8 更新为 1.5；旧牌回到替补区，不会重复出现在出战区。
- 未保存切换图鉴时出现保存/放弃/继续编辑选项，实际使用“保存并继续”，重新进入卡组后保留替换。
- 实际验证还原按钮与单独保存按钮；测试结束已恢复 QA 卡组原配置。
- 仅初始 6 张卡的存档显示无替补提示与首通获取途径，无可用修改时保存、还原按钮禁用。
- 收藏册实际显示独立大图、9/9 收集进度、具体获取条件、完整法术效果、搭配提示与缩略图翻阅；末张的下一张按钮禁用。
- 小屏完整显示主要操作。截图：`screenshots/deck-workshop.png`、`screenshots/card-library.png`。
- 未修改对战规则与卡牌数值，本次以构建和实际 UI 流程验证为主，未重复运行对战状态机测试。

## 独立设置页面

- 大厅设置入口改为跳转到独立页面，带返回按钮、原生触感/动态效果开关和玩法说明入口。
- Debug 模拟器构建通过；SE 模拟器确认全部设置内容无需滚动即可显示。
- 实际切换两个开关、返回大厅并重新进入后状态保留；验证后恢复原值。玩法说明可打开，关闭后返回设置。
- 本次为低影响 UI 修改，采用构建与交互检查，未新增或重复运行游戏逻辑测试。
- 截图：`screenshots/settings-page.png`。

## 能量风暴独立插画更新

- 使用独立 EnergyStorm 资源，紫蓝雷暴插画、紫色卡框和雷云符号；火花仍使用原黄色插画。
- 本次只修改美术映射与样式，未修改卡牌费用、效果或存档结构；未新增或重复运行逻辑测试。
- Debug 模拟器构建成功，在 SE 测试卡组中实际检查两张牌同时显示、资源加载及缩小后的辨识度。
- 截图：`screenshots/spark-and-storm.png`。新插画和提示词位置见 README 美术资源段。

## 构建

- Xcode 26.6，iOS Simulator 26.5 SDK。
- Scheme: Gaibreanno，Configuration: Debug。
- 使用 simulator SDK，构建时 `CODE_SIGNING_ALLOWED=NO`；没有改动项目中的签名设置。
- 最终结果：`BUILD SUCCEEDED`。
- 无应用源代码编译警告。Xcode 输出一条 App Intents 元数据提取跳过提示，原因是本项目不使用 AppIntents.framework。
- 主项目的 Bundle ID、自动签名方式、部署目标及 Debug / Release 配置保持不变。

## 游戏状态机

执行 `./scripts/check-game.sh`，结果：

```
PASS: 320 game engine checks, including all six encounters and save round-trip.
```

覆盖费用、卡槽限制、无效操作不扣费、成长、未来等待及首次翻倍、护盾与致命伤害顺序、回溯、资源上限、卡牌守恒、普通与困难三星路线、不同奖励组合、穿甲、重击、缺能、首领狂暴碎盾、困难独立规则、星级隔离、解锁与存档兼容。

## 直接选关与困难模式更新

- 最新构建成功。应用可见文案去掉章节层级，编号改为 01–06。
- 320 项状态机检查覆盖普通后两关的三种奖励组合，以及六个困难关卡的合法三星路线；旧版“六关初始卡组统一打法”已被新的后两关策略测试替代。
- 验证困难解锁门槛、两套星级隔离、困难不重复发卡、难度持久化及旧六关存档字段缺省兼容。
- 独立 `Time Cards Difficulty QA` SE 模拟器使用专用全通关测试存档，验证难度入口，不更改原测试设备的进度。
- 实际切换困难、翻页、进入甲虫关，说明同时展示基础和追加规则；奇数回合向现在出牌被拒绝且不扣能量，偶数回合可出牌。
- 实际以 4 回合、12 点生命取得困难甲虫三星；领取后困难为 1/6、3/18 星，无新卡奖励。
- 原 iPhone 17 Pro 存档更新后保留普通 2/6、6/18 星，困难入口保持未解锁；无章节名称和编号。
- `screenshots/direct-levels.png` 为更新后的普通选关页。
- `screenshots/difficulty-levels.png` 为测试存档下的实际困难 UI 截图。
- 本次未人工在 UI 中打通全部十二个普通/困难对局，通关可达性由明确合法操作序列测试覆盖。

## 上一版六关更新的模拟器复核

- iPhone 17 Pro：原型存档迁移后保留原卡组与已收集的时空巨龙；显示第一关完成，后续章节编号不误映射。
- 章节页：大屏每页 3 关、SE 每页 2 关；实际点击翻到最后一页，下一页禁用，规则、挑战、敌人插画与按钮完整显示。
- 实际游玩 1-2 铁皮树桩：成长单位正常穿甲，未来幼龙连击生效，护盾与当前敌方状态正确展示。
- 更新安装后续玩：恢复第 3 回合、玩家生命 16、敌方生命 16、三个已部署单位。
- 1-2 实际取得三星；奖励页显示回合、剩余生命、抵挡伤害与专属挑战结果。选择能量风暴仅加入收藏后，1-3 解锁、1-2 显示三星，总星级更新。
- 普通关卡重玩：1-1 实际以 3 回合、16 点生命取得三星，无新卡结算页正常显示，完成挑战可返回大厅并保存。
- SE 大厅主操作仍完整显示；新增章节末页已保存实际截图。
- 六关完整通关和三星可达性由状态机测试覆盖，本次未在 UI 中逐关打通六关。

## 原型阶段已完成的模拟器交互

以下记录来自更新前的三关版本，用于说明基础交互已验证；新章节复核见上节。


### iPhone 17 Pro / iOS 26.5

- 大厅、设置图标、卡牌插画及底部导航正常显示。
- 点选手牌、选择时间槽、能量扣除、卡牌离开手牌正常。
- 实际拖放种子炮台至过去、发条幼龙至现在成功。
- 将护盾拖到过去会显示原因，手牌与能量保持不变。
- 种子炮台在敌方行动后成长，插画、名称与攻击数值更新。
- 护盾、未来卫士、敌人强化回合和战斗记录可用。
- 实际打通第一关和第二关。
- 通关选牌、替换原卡、收藏保留、下一关解锁正常。
- 终止并重新运行应用后，奖励卡、已通关关卡、当前回合、生命、能量及场上成长状态恢复正常。

### iPhone SE (3rd generation) / iOS 26.5

- 使用本项目新建的 `Time Cards SE` 模拟器验证。
- 紧凑大厅无需滚动即可看到开始牌局和底部导航。
- 战场的卡槽、手牌、玩家状态和结束回合按钮完整显示。
- 空过回合至失败后，重试、调整卡组和返回大厅入口正常。
- 取消一张卡后，保存按钮变为禁用；恢复六张后可保存。
- 图鉴与未解锁卡牌详情正常，关闭后返回原页面。

## 第一章更新截图

- `screenshots/chapter-one-map.png`：大屏章节首页及真实解锁、星级。
- `screenshots/chapter-one-se.png`：SE 章节末页与首领。
- `screenshots/chapter-one-reward.png`：三星胜利与里程碑奖励。

## 原型阶段截图

- `screenshots/iphone17-lobby.png`
- `screenshots/iphone17-battle.png`
- `screenshots/iphone17-victory.png`
- `screenshots/iphone-se-lobby.png`

截图来自实际运行的模拟器，不是设计原型。

## 验证边界

- 未在物理 iPhone 上测试。
- 当前环境只安装了 iOS 26.5 模拟器；保留项目 iOS 14 部署目标，但未运行旧版系统。
- 当前交付为本地单机版本，不包含支付、广告、联网、账号或云同步。
